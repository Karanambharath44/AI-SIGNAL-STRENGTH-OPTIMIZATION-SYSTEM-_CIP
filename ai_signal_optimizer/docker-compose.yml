version: "3.9"

services:
  signal-optimizer:
    build: .
    container_name: ai_signal_optimizer
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./outputs:/app/outputs
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
